// Baked-in build version. Bump this together with public/version.json on every
// release. Older clients keep their old APP_VERSION, fetch the new version.json,
// see a different version string and show the "update available" prompt.
export const APP_VERSION = "1.0.1-beta"
